-- Example baseline migration (adapt to your existing schema)
-- If schema already exists, keep this as empty or idempotent statements
-- This file is a placeholder to enable Flyway; replace with your real DDL

-- ALTER TABLE MEMBER ADD COLUMN IF NOT EXISTS PHOTO_KEY VARCHAR(255);


